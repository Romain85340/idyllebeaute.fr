<nav class="navbar fixed-top navbar-expand-lg"> 
    <img src="/images/logo/IdylleBeauté5.png" alt="">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"><i class="fas fa-bars" style="color:#f2d6e8; font-size:28px;"></i></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/#activity">Services<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="/#contact">Contact<span class="sr-only">(current)</span></a>
        </li>
        <% if(userSession.userID > 0) { %>
          <% if(userSession.roleID === 1) { %>
            <li class="nav-item active">
              <a class="nav-link" href="/admin">Admin<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
              <a class="nav-link" href="/auth/logout">Déconnexion<span class="sr-only">(current)</span></a>
            </li>
          <% } else { %>
            <li class="nav-item active">
              <a class="nav-link" href="/user-area/<%= userSession.userID %>">Espace personnel de <%= userSession.userNAME %> <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
              <a class="nav-link" href="/auth/logout">Déconnexion<span class="sr-only">(current)</span></a>
            </li>
          <% } %>
        <% } else{ %>
        <li class="nav-item active">
          <a class="nav-link" href="/auth/login">Connexion<span class="sr-only">(current)</span></a>
        </li>
        <% } %>
      </ul>
    </div>
</nav>